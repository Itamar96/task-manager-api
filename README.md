Now we are making a server that waits for requests.(Converting Script-based to real RESTFUL API)

Postman → Express → Mongoose → MongoDB Atlas → Response


Postman sends the request without needing a frontend
Express receives it
Mongoose validates and stores or retrieves data
MongoDB Atlas keeps the data
Express sends the response back


Client -> Request -> Server -> Response